[project]
name = "markaz-scraper"
version = "0.1.0"
requires-python = ">=3.12"
description = "Markaz product scraper - Ultra-minimal for Vercel (Playwright only)"
dependencies = [
    "playwright==1.40.0"
]
