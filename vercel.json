{
  "version": 2,
  "installCommand": "pip install -r requirements.txt && playwright install chromium",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index"
    }
  ],
  "functions": {
    "api/*.py": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
